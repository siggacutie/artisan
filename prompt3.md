in the orders history tab the prices arent shown as configured in admin panel fix that also check if it deducts the correct amount as configured in admin panel and listen in mlbb topup page or does it detect the old amount?