# Specification: Show Prisma Schema

## Objective
The objective of this track is to provide the user with the current state of the `prisma/schema.prisma` file.

## Requirements
- Read the content of `prisma/schema.prisma`.
- Display the content to the user.
